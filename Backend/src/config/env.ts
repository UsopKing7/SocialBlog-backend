export const SAL = Number(process.env.SAL) || 10
export const PORT = Number(process.env.PORT) || 3333
export const SECRET = String(process.env.SECRET)
export const REDIS = String(process.env.URL_REDIS)
